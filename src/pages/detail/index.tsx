import React, { useState, useEffect, useCallback } from "react";
import { useParams } from "react-router-dom";

import { Container } from "./styles";

const Detail: React.FC = () => {
  return <Container>Detalhe</Container>;
};

export default Detail;
